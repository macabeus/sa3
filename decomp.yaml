platform: gba

tools:
  asmlift:
    # the DERIVED elf: a copy of sa3.elf with the DWARF types-sidecar merged in
    # (tools/asmlift-sidecar.sh) — names from .symtab, shapes from the DWARF
    elf: sa3-syms.elf
